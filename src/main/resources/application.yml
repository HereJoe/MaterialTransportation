server:
  port: 80
  servlet:
    context-path: /shortestPath
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/team?serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
